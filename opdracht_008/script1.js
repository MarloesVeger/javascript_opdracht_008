const numberCheck = function (number1, number2) {
    const sum = number1 + number2;
    if (sum > 100) {
        return true;
    }
    else if (sum < 100) {
        return false;
    }
}
const result = numberCheck(75, 26);
console.log(result);
// To calculate something

